* git init
* git add .
* git add app/*
* git add *.txt
* git status
* git status -s
* git commit
* git commit -m "版本紀錄的說明文字"
* git log 取得版本資訊
* git log -10
* git rm '*.txt'   刪除
* git rm 'app/*.html'
* git rm --cached a.txt 刪除git 索引檔,不刪除檔案
* git mv 'oldname' 'newname'  重新命名
* git reset
* git reset --hard
* git branch [BranchName] 建立分支
* git checkout [BranchName]   切換分支
* git checkout -b [BranchName] 建立並切換到新分支
* git branch -d [BranchName] 刪除分支
* git diff [commit1 old]  [commit2 new]   比對版本間的差異.前為舊.後為新
